# 🍌 Нана Банана - AI Photo Studio

Веб-приложение для генерации высококачественных изображений с помощью AI через Nano Banana API.

## Возможности

- 🎨 8 готовых пресетов для разных типов фотографий
- 🖼️ Генерация изображений в разрешении 2K и 4K
- 🎯 Выбор соотношения сторон (Авто, 1:1, 2:3, 3:2, 4:3, 16:9)
- ✨ Выбор стилей изображения
- 📦 Генерация нескольких изображений за раз (1, 2 или 4)
- 💾 Скачивание сгенерированных изображений

## Установка и настройка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка API

1. Получите API ключ Google Generative AI:
   - Перейдите на [Google AI Studio](https://aistudio.google.com/app/apikey)
   - Создайте новый API ключ или используйте существующий
   
2. Создайте файл `.env` в корне проекта на основе `.env.example`:

```bash
cp .env.example .env
```

3. Откройте `.env` и укажите ваш API ключ:

```env
VITE_GOOGLE_API_KEY=your_api_key_here
```

**Параметры:**
- `VITE_GOOGLE_API_KEY` - ваш Google Generative AI API ключ (обязательно)
- Приложение использует модель **Imagen 3.0** для генерации изображений

### 3. Запуск приложения

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

## Использование

1. **Выберите пресет** - выберите подходящий стиль из доступных пресетов
2. **Опишите изображение** - введите промпт с описанием того, что хотите увидеть
3. **Выберите стили** (опционально) - выберите дополнительные стили для изображения
4. **Настройте параметры**:
   - Разрешение (2K или 4K)
   - Соотношение сторон (или оставьте "Авто")
   - Количество изображений
5. **Сгенерируйте** - нажмите кнопку "Сгенерировать фото"

## Примечания по API

### Проблема CORS

Google Generative AI API **не поддерживает прямые запросы из браузера** из-за политики безопасности (CORS). 

**В режиме разработки:** Приложение использует Vite proxy для обхода CORS (настроено в `vite.config.ts`).

**В production:** Необходимо использовать бэкенд-сервер для проксирования запросов к Google API.

### Решения для Production

1. **Создать бэкенд-прокси** (рекомендуется):
   - Node.js/Express сервер
   - Python/Flask сервер
   - Любой другой бэкенд, который будет делать запросы к Google API

2. **Использовать другой API**, который поддерживает CORS

3. **Использовать Google Cloud Functions** или другой serverless сервис

### Формат ответа API

Проект использует **Google Gemini API (Nano Banana)** для генерации изображений:
- **Модель по умолчанию**: `gemini-2.5-flash-image` (для 2K разрешения)
- **Модель для 4K**: `gemini-3-pro-image-preview` (для 4K разрешения)

Формат ответа API:
```json
{
  "candidates": [{
    "content": {
      "parts": [{
        "inlineData": {
          "mimeType": "image/png",
          "data": "<base64_encoded_image>"
        }
      }]
    }
  }]
}
```

Если формат ответа API отличается от ожидаемого, проверьте:
- Формат ответа в консоли браузера (F12)
- Обновите функцию `generateImage` в `src/services/nanobananaApi.ts` под актуальный формат API

## Пресеты

- **Портрет 4K** - Профессиональные портреты с кинематографическим светом
- **Пейзаж 4K** - Эпические пейзажи с невероятной детализацией
- **Продукт 2K** - Студийная съёмка товаров для маркетплейсов
- **Цифровое искусство 4K** - Арт в стиле концепт-арта и цифровой живописи
- **Еда 2K** - Аппетитные фотографии блюд для ресторанов
- **Мода 4K** - Фэшн-фотография для журналов и каталогов
- **Архитектура 4K** - Архитектурная визуализация и экстерьеры
- **Аниме 2K** - Высококачественные иллюстрации в стиле аниме

## Технологии

- React 19
- TypeScript
- Vite
- Tailwind CSS
- Framer Motion
- Google Gemini API (Nano Banana) - генерация изображений через `gemini-2.5-flash-image` и `gemini-3-pro-image-preview`

## Лицензия

MIT
